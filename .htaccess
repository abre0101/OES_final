DirectoryIndex index.php index.php

# Redirect root to index.php
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # If accessing root directory, redirect to index.php
    RewriteRule ^$ index.php [L]
</IfModule>
